export interface EventBanner {
  id: string;
  title: string;
  previewImage: { filePath: string };
  // Add other relevant event properties if any
}

// You might add other event-related types here later if needed 